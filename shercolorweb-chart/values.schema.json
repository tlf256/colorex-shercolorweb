{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "required": ["env"],
    "properties": {
        "store": {
            "required": ["enabled"],
            "properties": {
                "enabled": {
                    "type": "boolean"
                }
            }
        },
        "env": {
            "type": "string",
            "enum": ["dev","qa","prod"]
        }
    },
    "title": "Values",
    "type": "object"
}