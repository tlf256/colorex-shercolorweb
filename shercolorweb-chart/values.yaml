# Default values for shercolorweb.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
appRoot: /shercolorweb

replicaCount: 3

image:
  repository: docker.artifactory.sherwin.com/colorex/shercolorweb
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

imagePullSecrets: []

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 8090

ingress:
  enabled: false
#  className: ""
#  annotations:
#    kubernetes.io/ingress.class: nginx
#    nginx.ingress.kubernetes.io/affinity: "cookie"
#    nginx.ingress.kubernetes.io/session-cookie-name: "INGRESSID"
#    nginx.ingress.kubernetes.io/session-cookie-expires: "172800"
#    nginx.ingress.kubernetes.io/session-cookie-max-age: "172800"
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
#  hosts:
#    - host: chart-example.local
#      paths:
#        - path: /
#          pathType: ImplementationSpecific
#  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
   limits:
     cpu: 500m
     memory: 1500Mi
   requests:
     cpu: 500m
     memory: 1200Mi

autoscaling:
  enabled: false

nodeSelector: {}

tolerations: []

affinity: {}

# Values for the postgresql dependency chart
shercolorPostgres:
  global:
    imageRegistry: docker.artifactory.sherwin.com
    storageClass: "longhorn"
    #TODO add different environment configs or change these creds?
    postgresql:
      postgresqlDatabase: "sherd"
      postgresqlUsername: "shercolor"
      existingSecret: "pg-secret"
      servicePort: "5432"
  image:
    registry: docker.artifactory.sherwin.com
    tag: 14.1.0-debian-10-r9
  persistence:
    size: 4Gi

# Conditional attribute only meant to be set via the Helm install command. 
# This attribute should be used to enable all kubernetes resources needed for store usage.
store:
  enabled: false

env: dev

# Global and environment-specific SherColor configurations 
oeBaseUrl: "http://stscpw01.sherwin.com/sw/rest/SherColor"

dev:
  dbOracleUrl: "jdbc:oracle:thin:@SHERD_DBHOST.sherwin.com:1521/sherd"
  dbOracleUsername: "shercolor"
  dbOraclePassword: "sh3rclrd3v"
  sherlinkLoginUrl: "https://cdweb.sherwin.com/sher-link/user/login.do"
  sherlinkTokenSwUrl: "https://cdweb.sherwin.com/sher-link/StoresEcommerceService"

qa:
  dbOracleUrl: "jdbc:oracle:thin:@SHERQ_DBHOST.sherwin.com:1521/sherq"
  dbOracleUsername: "SHERCOLOR_USER"
  dbOraclePassword: "sh3rqausr"
  sherlinkLoginUrl: "https://wqweb1.sherwin.com/sher-link/user/login.do"
  sherlinkTokenSwUrl: "https://wqweb1.sherwin.com/sher-link/StoresEcommerceService"

prod:
  dbOracleUrl: "jdbc:oracle:thin:@SHERP_DBHOST.sherwin.com:1521/sherp"
  dbOracleUsername: "SHERCOLOR_USER"
  dbOraclePassword: "sh3ruserp"
  sherlinkLoginUrl: "https://cdweb.sherwin.com/sher-link/user/login.do"
  sherlinkTokenSwUrl: "https://cdweb.sherwin.com/sher-link/StoresEcommerceService"